Hash[]
hash.fetch(:key)
Date.today.to_s == Date.today.strftime("%Y-%m-%d")
What does [*1..10] do?
Review strftime syntax
arr#zip
.equal? vs == vs eql?
What does this return and why?: [1,2,3] - [4,2,3]
Inheritance of class variables
Some File stuff
Some Error stuff

What does each item in this array equal:
arr = [
  "a".to_i(36),
  "070".to_i(0),
  nil.to_i,
  "0b0001".to_i
]

What does this return and why?
a = [1, 2, 5, 7, 8]
b = [1, 3, 6, 7, 9]
c =  nil || a & b | a && a | b
p c

*11/26/2025*

p "Apple-Banana-Lemon".split /(-)/


$val = 0

class Count
  def self.up
    $val = $val + 1
    $val == 3 ? true : false
  end
end

[*1..10].select do
  Count.up
end

p $val

What is hash.default_proc?

hash.values vs hash.values_at

Know common has methods like:
clear, merge, to_h, dig, fetch, transform_values, invert
select, reject, compact, keys, values, key?

hoge = *"a"
hoge.class

What does arr.each_cons do?

What is the difference with arr.each_slice(num)?

arr = (1..30).to_a
container = []

arr.each_cons(7) do |i|
  container << i
end

arr.each_slice(7) do |i|
  container << i
end

p container.length

puts "10G".to_i, "G10".to_i

Why is this 6?
x = 0

4.times do |i|
  x += i
end

p x

What does this return and why?
hoge = 0
def hoge
  x = 0
  5.times do |i|
    x += 1
  end
  x
end
puts hoge

What to put as arg to Date.today.strftime(arg) to get YYYY-MM-DD? MM/DD/YY?

strftime(%D) vs strftime(%x)

Know these:
strftime(%a)
strftime(%A)
strftime(%w)
strftime(%b)
strftime(%B)
strftime(%p)
strftime(%Z)


What does this puts?
def hoge(n)
  unless n != 3
    "hello"
  elsif n == 5
    "world"
  end
end

str = ''
str.concat hoge(3)
str.concat hoge(5)

puts str

Know string formatting like:
p "Hello%d" % 5

Common string placeholders
%d
%s
%f
%x

What method is the same as arr.delete_if?

Know File class methods like:
File.chmod
File.delete

What does hash.member? do?

What does hash.update do?

What does raise ['Error Message'] result in and why?

a, = (1..5).partition(&:odd?)
p a

Which mode in XXXX to get a file with
```
recode 1
recode 2?
```
File.open('testfile.txt', XXXX) do |f|
  f.write("recode 1\n")
  f.seek(0, IO::SEEK_SET)
  f.write("recode 2\n")
end

What does this return?
# list.txt
1
2
3
4

while not io.eof?
  io.readlines
  io.seek(0, IO::SEEK_CUR)
  p io.readlines
end

What does this return?
# text.txt
REx
Silver
REx
Gold

IO.read("text.txt", 3, offset = 1)

Know the different heredoc options:
<<-EOF
<<~EOF
<<EOF

